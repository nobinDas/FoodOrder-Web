To run the webpage first open the terminal with the folder the file is in. then write npm istall to install the required module and then npm start to run the website. And also mongod dbpath="batabase".

My webpage is not fully functional. It does not take or store Order data.